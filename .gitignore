.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

terraform.tfvars
*.auto.tfvars
*.tfvars

.vscode/
*.exe

# SSH keys (never commit private keys)
azure_key
azure_key.pub

# local logs/transcripts (optional)
proofs/**/00_transcript.log

# SSH keys (never commit)
azure_key
azure_key.pub

