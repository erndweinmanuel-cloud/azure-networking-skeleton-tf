# Terraform local stuff
.terraform/
*.tfstate
*.tfstate.*
terraform.tfstate.backup
crash.log
crash.*.log

# tfvars (never commit)
terraform.tfvars
*.tfvars
*.auto.tfvars

# Plan files (often contain IDs)
tfplan
*.tfplan

# SSH keys (never commit)
azure_key
azure_key.pub
*.pem
*.ppk
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# VS Code / OS
.vscode/
.DS_Store
Thumbs.db

# Proofs: ignore everything by default
proofs/**

# Allow ONLY public curated screenshots + redacted audit outputs
!proofs/docs-proofs/**
!proofs/audit/**

# But never allow raw dumps even inside allowed paths
proofs/audit/**/raw/**

# Optional: keep empty folders (only inside proofs)
!proofs/**/.gitkeep
