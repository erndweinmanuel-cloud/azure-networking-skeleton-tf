# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# tfvars (never commit)
terraform.tfvars
*.auto.tfvars
*.auto.tfvars.json

# Plans / outputs
tfplan
tfplan.*
*.plan
*.out

# SSH keys / certs
*.pem
*.ppk
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.key

# Local tooling
.vscode/
.DS_Store
Thumbs.db

# Proofs: keep only curated outputs you explicitly want
# (optional) uncomment if you want to never commit raw proof dumps
# proofs/**/raw/
